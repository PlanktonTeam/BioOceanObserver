## Header title
# Tab title

# function for UI module

HeaderTabUI <- function(id){
  
  nsHeaderTab <- NS(id)
  
  tagList(
    
  )
}

# function for server

HeaderTab <- function(id){
  moduleServer(
    id,
    function(input, output, session) {
      
      
    }
  )
}
    